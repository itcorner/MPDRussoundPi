---
- name: Deploy Russound web server
  hosts: musicpi
  become: true
  vars:
    deploy_user: "{{ ansible_user | default('musicpi') }}"
    app_dir: "/home/{{ deploy_user }}/MPDRussoundPi"
    venv_dir: "/home/{{ deploy_user }}/russound-venv"
    russound_host: "0.0.0.0"
    russound_port: 8000
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: Install base OS packages
      apt:
        name:
          - git
          - python3
          - python3-pip
          - python3-venv
          - python3-dev
          - build-essential
          - ser2net
        state: present
        update_cache: true

    - name: Ensure target deployment directory exists
      file:
        path: "{{ app_dir }}"
        state: directory
        owner: "{{ deploy_user }}"
        group: "{{ deploy_user }}"
        mode: "0755"

    - name: Clone or update repository
      git:
        repo: https://github.com/itcorner/MPDRussoundPi.git
        dest: "{{ app_dir }}"
        version: main
        force: true
        update: true

    - name: Ensure the application files are owned by the deployment user
      file:
        path: "{{ app_dir }}"
        state: directory
        owner: "{{ deploy_user }}"
        group: "{{ deploy_user }}"
        recurse: true

    - name: Create Python virtual environment
      command: "{{ ansible_python_interpreter }} -m venv {{ venv_dir }}"
      args:
        creates: "{{ venv_dir }}/bin/python"

    - name: Install Python dependencies
      pip:
        requirements: "{{ app_dir }}/web/requirements.txt"
        virtualenv: "{{ venv_dir }}"

    - name: Copy web config example if config missing
      copy:
        src: "{{ app_dir }}/web/config_example.json"
        dest: "{{ app_dir }}/web/russound_config.json"
        remote_src: true
        force: false
        owner: "{{ deploy_user }}"
        group: "{{ deploy_user }}"
        mode: "0644"

    - name: Ensure temporary state directory exists
      file:
        path: /tmp
        state: directory
        mode: "1777"

    - name: Copy ser2net configuration file
      copy:
        src: "{{ app_dir }}/mpdrussoundpi/ser2net/ser2net.yaml"
        dest: /etc/ser2net.yaml
        remote_src: true
        mode: "0644"

    - name: Install systemd service file
      template:
        src: ../templates/russound-web.service.j2
        dest: /etc/systemd/system/russound-web.service
        mode: "0644"

    - name: Reload systemd daemon
      systemd:
        daemon_reload: true

    - name: Enable and start ser2net
      systemd:
        name: ser2net
        enabled: true
        state: restarted

    - name: Enable and start russound web service
      systemd:
        name: russound-web
        enabled: true
        state: restarted
