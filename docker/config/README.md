Mount point for the Russound web configuration.

Copy `web/config_example.json` to `russound_config.json` in this directory
before the first start; the container reads it from `/config/russound_config.json`.
