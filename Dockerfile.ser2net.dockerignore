# Build context for Dockerfile.ser2net: only the gateway entrypoint is needed.
.git/
.github/
.venv/
.vscode/
**/__pycache__/
**/*.pyc

ansible/
docs/
mpdrussoundpi/
tests/
tool/
web/

# Runtime mount points; docker/entrypoint-ser2net.sh must stay in the build context.
docker/config/
docker/data/
docker/dummy-data/

**/*.log

docker-compose*.yml
Dockerfile*
.dockerignore
.env
